name: Testing Action
permissions: read-all

on: [workflow_dispatch]

jobs:
  scan-main:
    name: Scan code with HLint (main)
    runs-on: ubuntu-latest
    permissions:
      # Needed to upload results to GitHub code scanning.
      security-events: write
    steps:
      - uses: actions/checkout@8f4b7f84864484a7bf31766abe9204da3cbe65b3 # v3.5.0
      - uses: haskell-actions/hlint-scan@35c553f71a3484d1fe57f093cf221f68b8a502c4 # main

  scan-v0:
    name: Scan code with HLint (v0)
    runs-on: ubuntu-latest
    permissions:
      # Needed to upload results to GitHub code scanning.
      security-events: write
    steps:
      - uses: actions/checkout@8f4b7f84864484a7bf31766abe9204da3cbe65b3 # v3.5.0
      - uses: haskell-actions/hlint-scan@35c553f71a3484d1fe57f093cf221f68b8a502c4 # v0
